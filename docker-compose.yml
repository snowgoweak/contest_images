version: "3.8"

services:

  postgres:
      image: postgres
      container_name: photos_db
      environment:
          - POSTGRES_USER=postgres
          - POSTGRES_PASSWORD=postgres
          - POSTGRES_DB=postgres
      ports:
          - 5432:5432